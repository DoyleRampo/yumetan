# App Store 審査「Guideline 2.1 – Information Needed」対応プラン

作成日: 2026-09-25 / 更新: 2026-09-26（`develop` の実装に合わせて改訂）/ 対象: ユメタン iOS（`com.doyle.yumetan`）

## 1. フィードバックの正しい解釈

- **却下理由はバグや規約違反ではない。** 「App Review 履歴が少ない開発者アカウントからの新規提出なので、審査を完了するために追加情報が必要」という定型の情報要求（Information Needed）。7項目に答えれば審査が再開される。
- ただし、**要求項目はそのまま審査基準のチェックリスト**になっている。回答を用意する過程で以下の必須要件を満たしていないと、次は該当ガイドラインで却下される。
  - アカウント作成があるアプリは **アプリ内からのアカウント削除** が必須（Guideline 5.1.1(v)）。
  - サブスクリプション購入画面には **各プランの名称・期間・価格**、および **利用規約（EULA）とプライバシーポリシーへのリンク** が必須（Guideline 3.1.2 / Schedule 2）。
  - ユーザー生成コンテンツ（UGC）には **通報とブロック** の仕組みが必須（Guideline 1.2）。
  - ログインが必要な機能は **審査用デモアカウント** を App Store Connect に登録（Guideline 2.1）。
  - App 内課金商品はバイナリと **同時に提出** されていること（Guideline 3.1.1）。
- 回答は App Store Connect の「Reply」だけでなく、**App Review Information の Notes 欄にも同じ内容を記載**する（次回以降の提出で参照されるため）。
- 「Prevent Common Issues」節は一般的な注意喚起であり、個別の指摘ではない。ただしスクリーンショット（2.3.3）と IAP 同時提出（3.1.1）は該当しうるので事前確認する。

## 2. 現状（`develop`）とのギャップ

| # | Apple の要求 | `develop` の現状 | 対応 |
| --- | --- | --- | --- |
| 1a | 実機での画面録画（起動→典型フロー） | 未作成 | [OWNER_TASKS.md](OWNER_TASKS.md) §5 の台本で撮影 |
| 1b | 登録・ログイン・**アカウント削除** の録画 | Google / Apple / LINE / メール のログインと、設定 → アカウント → 「アカウントを削除」が実装済み。今回、削除範囲を拡張（他人の投稿へのスタンプ、全期間の使用数、RevenueCat 購読者、再試行可能） | 実装済み。録画に含める |
| 1c | UGC の通報・ブロックの録画 | **「みんなの夢」にはコメントがなく、通報・ブロック機能は `develop` で削除されている**（投稿とスタンプのみ）。Guideline 1.2 は UGC を表示する以上、通報とブロック（または不快なユーザーを除外する手段）を求める | **要判断（§3-C）**: 通報・ブロックを復活させるか、審査への説明で「投稿は公開前に OpenAI の安全性確認を通し、サポート窓口から通報できる」と述べて通すかを決める。安全側は復活 |
| 1d | 課金フローの録画（名称・期間・価格・利用規約・プライバシーポリシーのリンク） | プラン詳細に価格・月/年、自動更新の注記、規約とポリシーのリンクが表示済み。今回、購入ボタン直上に「プラン名 · 期間（自動更新）· 価格」を追加し、実機ではストア表示価格を優先 | 実装済み |
| 2 | 目的・対象ユーザーの説明 | 英語の審査向け文章は [REVIEW_NOTES.md](REVIEW_NOTES.md) | 文案あり |
| 3 | セットアップ手順・ログイン情報 | デモアカウントなし。他人の投稿の全文閲覧・スタンプ・AI 読み解きは有料プラン限定 | OWNER_TASKS §3 |
| 4 | 外部サービス一覧 | REVIEW_NOTES に整理済み | 文案あり |
| 5 | 地域差 | 機能は全地域共通、4言語対応 | 文案あり |
| 6 | 規制業種・第三者著作物 | 医療機器ではない旨の免責あり。キャラクターは自作 AI 生成画像 | 文案あり |
| 7 | IAP の概要と導線 | 4つの自動更新サブスクリプション（RevenueCat 経由） | 文案あり |

## 3. コード側の対応状況

### A. アカウント削除 — 実装済み

- クライアント: 設定 → アカウント → 「アカウントを削除」→ 確認ダイアログ → サーバー削除 → 端末内の全アカウントキャッシュ消去 → 初回画面へ。
- サーバー `POST /api/account/delete`（`server/account.js`）: 本人の投稿（スタンプの副コレクション含む）、**他人の投稿に付けた本人のスタンプ（投稿側の集計も補正）**、`users/{uid}` 配下、会員情報、`communityStats`、**全期間の使用数**、**RevenueCat 購読者（`DELETE /v1/subscribers/{uid}`、失敗しても続行）**、監査用の UID ハッシュのみ記録、最後に Firebase Auth ユーザー。Auth ユーザーが既にいない場合の再実行も成功する。
- スタンプ文書に `owner` を保存し、`firestore.indexes.json` に `reactions.owner` のコレクショングループ設定を追加（要デプロイ）。
- 削除ではストアのサブスクリプションは解約されない。確認文と規約で App Store / Google Play からの解約を案内する（`deleteAccountConfirm` の文言に解約案内を足すとより親切）。

### B. 課金画面と法的ページ — 実装済み（リンク先の切替は要作業）

- プラン詳細の購入ボタン直上に「プラン名 · 月額/年額」「1か月（自動更新）· 価格」を表示。実機では RevenueCat の `getOfferings()` の `priceString` を優先。
- 利用規約とプライバシーポリシーのリンクは `public/core/help-content.js` の `HELP_LINKS`。現在は利用規約 = Apple 標準 EULA、プライバシーポリシー = 外部サイト（chatgpt.site）。
- 自前の `public/legal/terms.html` / `privacy.html`（日英）を追加した。**事業者名などのプレースホルダーを埋めたうえで**、`HELP_LINKS.terms` / `privacy` を `https://<サーバー>/legal/terms.html` / `privacy.html` に切り替える（OWNER_TASKS §1）。切り替えるまで既存リンクは動作するので審査上の支障はない。

### C. 通報・ブロック — 要判断

`develop` は「みんなの夢」からコメント・通報・ブロックを削除している。Apple は UGC を表示するアプリに「不快なコンテンツの通報」「不快なユーザーのブロック」の両方を求める（Guideline 1.2）。選択肢:

1. **復活させる（推奨）**: 旧実装（`main` ブランチの `server/community.js` の `report` / `block` ルートと `public/community.js` の UI）を移植する。投稿詳細に「通報」「この投稿者を非表示」を置き、ブロック一覧と解除を設定に置く。運営は `communityReports` を確認して `hidden=true` にする。
2. 説明で通す: 投稿前の OpenAI 安全性確認、運営による非表示、サポート窓口からの通報を Notes に書く。ブロック機能がないため却下のリスクが残る。

### D. 審査用デモアカウント — 本人作業

メール/パスワードのログイン欄は初回画面と設定に既にある。Firebase で「メール/パスワード」プロバイダを有効化し、デモアカウントを作る（OWNER_TASKS §2–3）。

### E. その他 — 実装済み

- `capacitor.config.json` の `appId` を Xcode の `com.doyle.yumetan` に統一。
- App Store のスクリーンショットが起動画面だけになっていないか確認（2.3.3）。

## 4. 提出物

[REVIEW_NOTES.md](REVIEW_NOTES.md) に、7項目の英語回答と画面録画の台本をまとめている。

## 5. 再提出前チェックリスト

- [ ] `develop` をサーバーへデプロイし、`/legal/privacy.html` と `/api/account/delete` が本番で動く
- [ ] 規約ページのプレースホルダーを埋め、`HELP_LINKS` を切り替える（または既存リンクのままにする判断）
- [ ] `firebase deploy --only firestore:indexes` を実行
- [ ] C の判断（通報・ブロックの復活）
- [ ] デモアカウント2つ（Standard 付与済み / Free）を「サインイン情報」と Notes に登録
- [ ] フィードにデモ投稿がある
- [ ] IAP 4商品をバイナリと同時提出、プライバシーポリシー URL を登録
- [ ] 実機（最新 iOS）で削除・購入・公開を確認し、画面録画を作成
- [ ] `npm test` / `npm run check` / `npm run test:browser` が通る
