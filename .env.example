# Server-only secrets. Never commit .env or put these in public/config.js.
OPENAI_API_KEY=
# Model for readings and OCR. Default: the gpt-5.6-luna alias (cost calculation in
# server/openai.js assumes Luna pricing). Pin a dated snapshot here if you need one.
OPENAI_MODEL=
# Tried once when the project cannot use OPENAI_MODEL (unknown id / no access).
# Default gpt-5.6-terra (about 10x Luna's price per call); set to "" to disable.
OPENAI_FALLBACK_MODEL=
AI_GLOBAL_MONTHLY_USD=20

FIREBASE_PROJECT_ID=yumetan-a31f0
FIRESTORE_DATABASE_ID=(default)
# JSON service-account credentials for the SAME project as public/firebase-config.js.
# Alternatively use GOOGLE_APPLICATION_CREDENTIALS / workload identity.
FIREBASE_SERVICE_ACCOUNT_JSON=

# RevenueCat (App Store / Google Play subscriptions). Both are server-only secrets.
# Secret API key (sk_...): RevenueCat → Project settings → API keys → Secret API keys.
REVENUECAT_SECRET_API_KEY=
# Any long random string; paste the same value as the webhook's Authorization header
# in RevenueCat → Integrations → Webhooks (URL: https://<server>/api/billing/revenuecat).
REVENUECAT_WEBHOOK_AUTH=
# Public SDK keys (appl_/goog_) are NOT set here; pass them to scripts/build-mobile.mjs.
# Test Store keys (test_) are Debug-only: a TestFlight / App Store build configured with one
# shows "Wrong API Key" and exits, so build-mobile.mjs rejects them unless REVENUECAT_ALLOW_TEST_STORE=1.
# LINE Login channel ID (public). Lets the iOS app log in through the LINE app
# directly; the server verifies the LINE ID token and issues a Firebase token.
# The channel must have "Mobile app" enabled with the iOS bundle ID registered.
LINE_CHANNEL_ID=
# Public support/contact page for community reports and billing enquiries.
SUPPORT_URL=
# PORT=3000
