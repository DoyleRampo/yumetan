rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 夢の記録は本人（ログイン中の uid）だけが読み書きできる
    match /users/{uid}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
